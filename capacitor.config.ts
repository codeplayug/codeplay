import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ug.codeplay.learning',
  appName: 'codeplay',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
